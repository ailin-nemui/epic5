# Emacs-like window handling
#
bind ^Xo next_window
bind ^X0 parse_command window kill
bind ^X1 parse_command window kill_others
bind ^X2 parse_command window new
bind ^X= parse_command window balance
bind ^X+ parse_command window grow 1
bind ^X- parse_command window shrink 1
bind ^Xz parse_command window grow 1
bind ^X^z parse_command window shrink 1
# bind ^X^ parse_command window grow 1
# bind ^X^^ parse_command window grow 1
